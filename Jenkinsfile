pipeline   {

    agent any

    stages {
        stage("build"){

            steps{
                echo "we in da build phase"
            }
        }
        
        stage("test"){

            steps{
                echo "we in da test phase"
            }
        }
        
        stage("deploy"){

            steps{
                echo "we in da deploy phase"
            }
        }
    }
}
